import 'package:flutter/material.dart';

class AppColors {
  static const dark = Colors.black;
  static const light = Colors.white;

  static const background = Color(0xFF16222E);
  static const onBackground = Colors.white;

  static const error = Color(0xFFB63623);
  static const success = Color(0xFF65A91A);
  static const disabled = Color(0xFF787878);
}
